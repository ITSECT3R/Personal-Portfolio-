#!/bin/sh
. "$(dirname "$0")/\_/husky.sh"

# Run lint-staged to lint and format staged files

npx lint-staged

# Generate updated project structure documentation

npm run generate-structure

# Stage the updated project structure file for commit

git add project/project-structure.md
